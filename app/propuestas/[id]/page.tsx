import Link from "next/link"; import {redirect} from "next/navigation"; import {ArrowLeft} from "lucide-react"; import {createClient} from "@/lib/supabase/server"; import ProposalActions from "./proposal-actions";
export const dynamic="force-dynamic";
export default async function ProposalPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:p}=await supabase.from("trade_proposals").select("id,proposer_id,recipient_id,status,message,created_at,proposer:profiles!trade_proposals_proposer_id_fkey(display_name,username),recipient:profiles!trade_proposals_recipient_id_fkey(display_name,username)").eq("id",id).single();if(!p)redirect("/mis-matches");const {data:items}=await supabase.from("trade_items").select("id,direction,sticker:stickers(number,name,team)").eq("trade_proposal_id",id);const other=user.id===p.proposer_id?p.recipient:p.proposer;const otherName=(Array.isArray(other)?other[0]:other)?.display_name||(Array.isArray(other)?other[0]:other)?.username||"Coleccionista";return <main className="min-h-screen bg-[#f5f2e9] px-4 py-8"><div className="mx-auto max-w-3xl"><Link href="/mis-matches" className="flex items-center gap-2 text-sm font-bold"><ArrowLeft size={18}/>Mis matches</Link><div className="mt-8 flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[#527060]">Propuesta con {otherName}</p><h1 className="mt-2 text-4xl font-black">Intercambio</h1></div><span className="rounded-full bg-white px-3 py-2 text-xs font-black uppercase">{p.status}</span></div>{p.message&&<p className="mt-5 rounded-xl bg-white p-4">“{p.message}”</p>}<div className="mt-5 grid gap-4 md:grid-cols-2"><ItemSide title={user.id===p.proposer_id?"Tú entregas":"Tú recibes"} items={(items??[]).filter(i=>i.direction==="offered")}/><ItemSide title={user.id===p.proposer_id?"Tú recibes":"Tú entregas"} items={(items??[]).filter(i=>i.direction==="requested")}/></div>{p.recipient_id===user.id&&p.status==="pending"&&<ProposalActions proposalId={p.id}/>}</div></main>}
type TradeItem={id:string;direction:string;sticker:{number:string;name:string|null;team:string}|{number:string;name:string|null;team:string}[]|null};
function ItemSide({title,items}:{title:string;items:TradeItem[]}){return <section className="rounded-2xl bg-white p-5"><h2 className="font-black">{title}</h2><div className="mt-3 space-y-2">{items.map(i=>{const s=Array.isArray(i.sticker)?i.sticker[0]:i.sticker;return <p key={i.id} className="rounded-lg bg-[#f0eee7] px-3 py-2 text-sm font-bold">#{s?.number} · {s?.name??s?.team}</p>})}</div></section>}
