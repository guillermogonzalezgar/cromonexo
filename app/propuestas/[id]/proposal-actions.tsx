"use client"; import {useMemo,useState} from "react"; import {useRouter} from "next/navigation"; import {Check,X} from "lucide-react"; import {createClient} from "@/lib/supabase/client";
export default function ProposalActions({proposalId}:{proposalId:string}){const supabase=useMemo(()=>createClient(),[]),router=useRouter();const [busy,setBusy]=useState(false),[message,setMessage]=useState("");const respond=async(response:"accepted"|"rejected")=>{setBusy(true);const {error}=await supabase.rpc("respond_trade_proposal",{p_proposal_id:proposalId,p_response:response});setBusy(false);if(error)setMessage("No se pudo responder.");else router.refresh()};return <div className="mt-5 grid grid-cols-2 gap-3"><button disabled={busy} onClick={()=>respond("rejected")} className="flex items-center justify-center gap-2 rounded-xl border border-[#173d2a]/20 bg-white p-4 font-bold"><X/>Rechazar</button><button disabled={busy} onClick={()=>respond("accepted")} className="flex items-center justify-center gap-2 rounded-xl bg-[#164f35] p-4 font-bold text-white"><Check/>Aceptar</button>{message&&<p className="col-span-2 text-sm font-bold">{message}</p>}</div>}
