import Link from "next/link";import {redirect} from "next/navigation";import {ArrowRight,Bell,CircleCheck,Repeat2,ShoppingBag,Sparkles} from "lucide-react";import AppHeader from "@/components/app-header";import {createClient} from "@/lib/supabase/server";
export const dynamic="force-dynamic";type Match={compatibility_score:number};
export default async function Dashboard(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:profile}=await supabase.from("profiles").select("display_name,username").eq("id",user.id).single();const {data:collection}=await supabase.from("collections").select("id").eq("slug","laliga-este-2026-27").single();const [{count:wanted},{count:duplicates},{data:matches},{count:proposals},{count:requests}]=await Promise.all([supabase.from("user_stickers").select("*",{count:"exact",head:true}).eq("user_id",user.id).eq("status","wanted"),supabase.from("user_stickers").select("*",{count:"exact",head:true}).eq("user_id",user.id).eq("status","duplicate"),collection?supabase.rpc("get_user_matches",{p_collection_id:collection.id}):Promise.resolve({data:[]}),supabase.from("trade_proposals").select("*",{count:"exact",head:true}).eq("recipient_id",user.id).eq("status","pending"),supabase.from("market_requests").select("*",{count:"exact",head:true}).eq("status","pending")]);const matchRows=(matches??[]) as Match[],alerts=(proposals??0)+(requests??0),name=profile?.display_name||profile?.username||"coleccionista";return <div className="min-h-screen pb-24 md:pb-12"><AppHeader active="home"/><main className="mx-auto max-w-6xl px-4 py-9 md:px-8 md:py-14"><section className="relative overflow-hidden rounded-[2rem] bg-[#173d2a] p-6 text-white shadow-[0_24px_60px_rgba(23,61,42,.2)] md:p-10"><div className="absolute -right-16 -top-20 h-72 w-72 rounded-full bg-[#c9f31d]/15 blur-2xl"/><p className="relative text-sm font-bold text-[#c9f31d]">Hola, {name}</p><h1 className="relative mt-2 max-w-2xl text-4xl font-black tracking-[-.055em] md:text-6xl">Tu colección está en movimiento.</h1><p className="relative mt-4 max-w-xl text-white/65">Revisa tus oportunidades, responde a otros coleccionistas y sigue acercándote a completar el álbum.</p><Link href="/mis-matches" className="relative mt-7 inline-flex items-center gap-2 rounded-xl bg-[#c9f31d] px-5 py-3 font-black text-[#173d2a]">Ver mis matches<ArrowRight size={18}/></Link></section><section className="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-4"><Stat icon={<Sparkles/>} value={wanted??0} label="Me faltan" tone="coral"/><Stat icon={<Repeat2/>} value={duplicates??0} label="Repetidos" tone="lime"/><Stat icon={<CircleCheck/>} value={matchRows.length} label="Matches activos" tone="green"/><Stat icon={<Bell/>} value={alerts} label="Avisos pendientes" tone="dark"/></section><div className="mt-8 grid gap-5 lg:grid-cols-[1.4fr_1fr]"><section className="surface rounded-3xl p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[.16em] text-[#6d7e73]">Actividad</p><h2 className="mt-1 text-2xl font-black">Necesita tu atención</h2></div>{alerts>0&&<span className="grid h-9 min-w-9 place-items-center rounded-full bg-[#ff6a3d] px-2 font-black text-white">{alerts}</span>}</div><div className="mt-5 space-y-3"><Action href="/mis-matches" icon={<Repeat2/>} title="Propuestas de intercambio" detail={proposals?`${proposals} pendiente${proposals===1?"":"s"}`:"Todo al día"}/><Action href="/mercado/solicitudes" icon={<ShoppingBag/>} title="Solicitudes del mercado" detail={requests?`${requests} pendiente${requests===1?"":"s"}`:"Sin solicitudes nuevas"}/></div></section><section className="rounded-3xl bg-[#e5efd9] p-6"><p className="text-xs font-black uppercase tracking-[.16em] text-[#287051]">Mejor oportunidad</p><p className="mt-3 text-5xl font-black">{matchRows[0]?.compatibility_score??0}</p><p className="mt-1 font-bold">cromos compatibles en tu mejor match</p><Link href="/mis-matches" className="mt-6 inline-flex items-center gap-2 text-sm font-black text-[#164f35]">Explorar compatibilidades<ArrowRight size={16}/></Link></section></div></main></div>}
function Stat({icon,value,label,tone}:{icon:React.ReactNode;value:number;label:string;tone:"coral"|"lime"|"green"|"dark"}){const colors={coral:"bg-[#fff0e9] text-[#ce4a24]",lime:"bg-[#eff8c8] text-[#536500]",green:"bg-[#e3f0e9] text-[#287051]",dark:"bg-[#e8ebe8] text-[#173d2a]"};return <article className="surface rounded-2xl p-4 md:p-5"><div className={`grid h-10 w-10 place-items-center rounded-xl ${colors[tone]}`}>{icon}</div><strong className="mt-4 block text-3xl font-black">{value}</strong><span className="text-xs font-bold text-[#718078]">{label}</span></article>}
function Action({href,icon,title,detail}:{href:string;icon:React.ReactNode;title:string;detail:string}){return <Link href={href} className="flex items-center gap-4 rounded-2xl border border-[#173d2a]/10 bg-[#fbfaf5] p-4 transition hover:bg-white"><span className="grid h-11 w-11 place-items-center rounded-xl bg-white text-[#287051] shadow-sm">{icon}</span><span className="min-w-0 flex-1"><strong className="block">{title}</strong><small className="text-[#718078]">{detail}</small></span><ArrowRight size={18}/></Link>}
