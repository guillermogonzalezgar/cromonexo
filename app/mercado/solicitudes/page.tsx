import Link from "next/link";import {redirect} from "next/navigation";import {ArrowLeft} from "lucide-react";import {createClient} from "@/lib/supabase/server";import RequestActions from "./request-actions";
export const dynamic="force-dynamic";
export default async function RequestsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:requests}=await supabase.from("market_requests").select("id,buyer_id,status,created_at,buyer:profiles!market_requests_buyer_id_fkey(display_name,username),listing:market_listings(seller_id,price_cents,sticker:stickers(number,name,team))").order("created_at",{ascending:false});return <main className="min-h-screen bg-[#f5f2e9] px-4 py-8"><div className="mx-auto max-w-3xl"><Link href="/mercado" className="flex items-center gap-2 text-sm font-bold"><ArrowLeft size={18}/>Mercado</Link><h1 className="mt-8 text-4xl font-black">Solicitudes de compra</h1><div className="mt-6 space-y-3">{(requests??[]).map(r=>{const listing=Array.isArray(r.listing)?r.listing[0]:r.listing;const sticker=listing?(Array.isArray(listing.sticker)?listing.sticker[0]:listing.sticker):null;const buyer=Array.isArray(r.buyer)?r.buyer[0]:r.buyer;const incoming=listing?.seller_id===user.id;return <article key={r.id} className="rounded-2xl bg-white p-5"><div className="flex justify-between gap-3"><div><p className="font-black">#{sticker?.number} · {sticker?.name??sticker?.team}</p><p className="mt-1 text-sm text-[#6b786f]">{incoming?`Solicitud de ${buyer?.display_name||buyer?.username||"Coleccionista"}`:"Tu solicitud de compra"}</p></div><div className="text-right"><p className="font-black">{listing?`${(listing.price_cents/100).toFixed(2).replace(".",",")} €`:""}</p><span className="text-xs font-bold uppercase">{r.status}</span></div></div>{incoming&&r.status==="pending"&&<RequestActions requestId={r.id}/>}</article>})}{!requests?.length&&<p className="rounded-2xl border border-dashed p-10 text-center">Todavía no tienes solicitudes.</p>}</div></div></main>}
